# PROGRESS

2026-07-06 Audited and fixed the core bug in mindmap-render: build_html injected markmap options into a `data-markmap` attribute — but inspecting the pinned markmap-autoloader@0.17.2 bundle shows the autoloader reads options ONLY from the markdown `markmap:` frontmatter (deriveOptions) and never touches dataset, so every option was dead code; worse, that `.replace()` also deleted `id="target-markmap"`, making loadMdAndRender's getElementById always null. Now the geometry options are delivered via frontmatter (colors are left to the skill's own post-processing to avoid a regression) and the id is preserved. Verified real renders of both air and editorial themes: editorial now honors paddingX:14 so card underlines no longer overshoot; 15/15 unit tests pass.

2026-07-03 Fixed three P0 issues and verified: install.sh rejects empty/path-like args (sandbox-tested); magnitude_consistency scales SI prefixes by the unit's power (15 cases + smoketest pass); fno-burgers ground truth switched to a fine-grid solve + spectral truncation (converges to float32 precision), full re-run 2072s — FNO 6.67%→4.41% (the difference was the artifact), the super-resolution band flattened to 4.2–4.7%, and the related docs' numbers were synced. Remaining: paper.pdf has no tex source and still carries the old numbers; P1/P2 bugs unfixed.

2026-07-03 Whole-repo bug audit (~5000 lines): confirmed 12 real bugs. P0: install.sh with an empty arg would wipe the user's skills directory (reproduced); fno-burgers' ν=1e-3@128 ground truth is under-resolved (measured 5.9% rel-L2 contamination, same order as the FNO's measured 6.7% error, so the headline result is distorted); magnitude_consistency's mm²/cm³ prefix conversion is off by 10–1000×. None fixed yet, pending.
